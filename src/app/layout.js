export const metadata = {
  title: 'Photo Gallery & Measurement',
  description: 'Upload and measure angles in photos',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
} 